name: Deploy website com push

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Obter o código mais recente
        uses: actions/checkout@v2

      - name: Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: "ftp.starttofinish.com.br"
          username: "Jose_dev_full_stack@starttofinish.com.br"
          password: "29_Zezao_DevFull" # sla
